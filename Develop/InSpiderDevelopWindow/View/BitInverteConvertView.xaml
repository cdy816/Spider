<UserControl x:Class="InSpiderDevelopWindow.View.BitInverteConvertView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InSpiderDevelopWindow.View"
             xmlns:res="clr-namespace:InSpiderDevelopWindow"
             mc:Ignorable="d" Name="cfg"  Foreground="{Binding Foreground,RelativeSource={RelativeSource AncestorType=UserControl}}"
             d:DesignHeight="120.626" d:DesignWidth="303.05">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Label Foreground="{Binding Foreground,ElementName=cfg}" Content="{res:ResMarker BitInvertefunTitle,:}" HorizontalAlignment="Left" Margin="5,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>
        <CheckBox Content="{res:ResMarker Enable}" Foreground="White" Background="Transparent" VerticalContentAlignment="Center" IsChecked="{Binding Enable,Mode=TwoWay}" HorizontalAlignment="Left" Margin="10,10,0,0" Grid.Row="1" VerticalAlignment="Top"/>

    </Grid>
</UserControl>
